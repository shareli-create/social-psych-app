import React from 'react'
import ReactDOM from 'react-dom/client'
import ReferenceChecker from './ReferenceChecker.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ReferenceChecker />
  </React.StrictMode>,
)
